/**
 * @docs `isUnknown`
 *
 * Given one or more statuses, this function will return whether that status includes `UNKNOWN`.
 *
 * If one is `UNKNOWN` the result will also be `UNKNOWN`
 */
export default function isUnknown(...statuses: number[]): boolean;
