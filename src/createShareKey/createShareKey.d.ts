/**
 * @docs `createShareKey`
 *
 * Creates a key to determine where to save an lookup fetches that are shared.
 *
 * > **Note**: This is an advanced API that you'll probably never use. Search in the tests for usage.
 *
 * Function signature:
 */
export default function createShareKey(namespace: string, key: string): string;
